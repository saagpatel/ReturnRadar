# Optional local fixture overrides. Do not use these values for a production
# build: the fixture bundle has a separate identifier and database container.
VITE_RETURNRADAR_DB_URL=sqlite:return_radar_fixture.db
VITE_RETURNRADAR_DISABLE_NOTIFICATIONS=true
